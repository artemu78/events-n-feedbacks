import { Paper, Stack, Typography } from '@mui/material';

const Page = () => {
  return (
    <Typography sx={{ mb: 2 }} variant="h4">
      Profile
    </Typography>
  );
};

export default Page;
