import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Events'n'Feedback / moderators",
  description: "Event, speakers and their feedback",
};

const Page = () => {
    return (
        <div>
            <h1>Moderators</h1>
        </div>
    )   
}

export default Page;